# AI-use log — 10-Day Challenge

One line per AI contribution. Three columns. This becomes a section of the case study.

| Date | What AI did | What I changed | What I decided |
|---|---|---|---|
| 2026-09-06 | Translated and organised the kickoff chat into a brief; ran a prior-art scan (hobby projects, mood-recommender papers, MoodDJ/EmoHeal, Affectiva/Cerence) | — | Framed the gap as: facial input + phase-transition model, documented as a design case, not a product |
| 2026-09-18 | Drafted the process map (loop-per-step, gates, AI-use log) and the 23-question survey | | |
| 2026-09-18 | Built the HealthySongs prototype: valence–arousal map, iso-principle planner, face→mood heuristic (MediaPipe blendshapes), manual check-in override, journey runner, research log export, laptop fallback sensor | | Chose Expo mobile app, local files, face + manual check-in, iso-principle journey |
| 2026-09-21 | Researched Jen (Futureverse text-to-music API: generate + status endpoints, 24 h file expiry, $0.04/60 s). Added the keep-or-compose selection engine: spectrum point → readable prompt; Jen composes only where the library has nothing close enough. Built an interactive web preview of the app | | Use Jen for selection, not as the whole library; mixed mode as default |
| 2026-09-21 | Re-read the project against its aim (a steadier state, not a louder bad one) and found the app could feed the feeling: the first track matched the mood exactly (angry → angry music), and if you got worse mid-journey it re-matched the worse state. Added regulate.ts: meet a step lighter, never play tense/heavy tracks, a Balance target that picks the direction from the state, and measuring what each track does (cut and remember tracks that make it worse). Same rules in the web demo. Added Spotify: sign-in, now-playing verdict, suggested next song, queue/play, auto-queue and replace-if-worse | | Regulation over matching: the iso-principle is only for being accepted, never for staying in the feeling |
| 2026-09-21 | Made Jen non-blocking: the journey starts immediately from the library and Jen composes the later steps while the first plays, swapping each in when ready. Saved the Spotify key to .env.local and added a check that warns when a value isn't a 32-character Client ID | | Deciding stays local and instant; Jen composes, it doesn't decide |
| 2026-09-21 | Phone setup on iPhone: LAN connection failed, switched to the Expo tunnel (ngrok installed in the project). Added an https relay page in metro.config.js so Spotify sign-in works (Spotify rejects exp:// redirects for new apps). Smoke-tested every screen in a browser: fixed a misleading "this step sits at heavy" label, the Balance header, the manual status line and the consent text, which still described matching the mood | | Tunnel for development; https relay for Spotify |
| 2026-09-21 | Admitted the first preview was a simulation (no camera, music only changed on map clicks). Rebuilt it as HealthySongs Live: real webcam expression reading (face-api, runs in the browser), continuous music that follows a match / lead / wait / re-meet rule, with a feed explaining each decision | | Judgment rule: music may lead at most 0.35 ahead of you; waits if you don't follow; re-matches if you shift |
| 2026-09-22 | Renamed the project from MoodPath to HealthySongs everywhere: display name, app slug and URL scheme, package name, the app folder, docs and the web demo. Earlier rows in this log use the new name. | | Chose the name HealthySongs |
